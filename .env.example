# ===============================================================
#    SwapMyBytes Environment Variables Template
# ===============================================================
#
# Kopiere diese Datei nach .env (cp .env.example .env) und
# fülle die leeren Werte aus.
# Diese .env-Datei wird von Docker Compose verwendet, um die
# gesamte Anwendung zu konfigurieren.
#

# --- Frontend Configuration ---
# Port, auf dem der Frontend-Server im Docker-Container läuft
SMB_FRONTEND_PORT_INTERNAL=80
# Port, über den das Frontend auf deinem Computer erreichbar ist
SMB_FRONTEND_PORT_EXTERNAL=5080

# --- Backend Configuration ---
# Port, auf dem der Backend-Server im Docker-Container läuft
SMB_BACKEND_PORT_INTERNAL=3000
# Port, über den das Backend auf deinem Computer erreichbar ist
SMB_BACKEND_PORT_EXTERNAL=5090
# Setze auf "true", um die Datenbank beim Start mit umfangreichen Demo-Daten zu füllen
SMB_BACKEND_POPULATE=false

# --- Database Configuration (MongoDB) ---
# Der Service-Name des MongoDB-Containers, wie in compose.yml definiert.
# Diesen Wert normalerweise nicht ändern.
SMB_MONGO_SERVICE_LOCATION=mongodb
# Interner Port für MongoDB im Docker-Netzwerk
SMB_MONGO_PORT_INTERNAL=27017
# Externer Port für MongoDB auf deinem Computer (falls direkter Zugriff benötigt wird)
SMB_MONGO_PORT_EXTERNAL=27017
# Name der Datenbank, die von der Anwendung verwendet wird
SMB_MONGO_DBNAME=swapmybytes
# Root-Benutzer für die Datenbank-Initialisierung
MONGO_INITDB_ROOT_USERNAME=mongoadmin
MONGO_INITDB_ROOT_PASSWORD=secret

# --- Database Admin Tool (Mongo Express) ---
# Port für das Mongo Express Web-Interface
SMB_MONGO_EXPRESS_PORT_EXTERNAL=8081
# Admin-Anmeldedaten für Mongo Express (sollten mit den Root-Credentials übereinstimmen)
ME_CONFIG_MONGODB_ADMINUSERNAME=mongoadmin
ME_CONFIG_MONGODB_ADMINPASSWORD=secret
# Verbindungs-URL, die Mongo Express verwendet, um sich mit der DB zu verbinden.
# Nutzt den Service-Namen und die internen Credentials.
ME_CONFIG_MONGODB_URL=mongodb://mongoadmin:secret@mongodb:27017/

# --- External Services & Secrets ---
# WICHTIG: Diese Werte müssen von Google Cloud bezogen werden.
# Lasse sie leer, wenn du die Google OAuth-Funktionalität nicht testen möchtest.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=